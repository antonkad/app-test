# app-test

Isolation canary for kad.dev (trial `indigo-panda`).

| Branch | Stack |
|--------|--------|
| `main` | Flask (server runtime + env) |

Bind `PORT` (kad default 8080). Do not log secrets.
